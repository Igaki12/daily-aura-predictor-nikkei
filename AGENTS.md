# AGENTS.md

## プロジェクト情報

- プロジェクト名 / リポジトリ名は **`daily-aura-predictor-nikkei`** を採用する
- Git リポジトリは **`https://github.com/Igaki12/daily-aura-predictor-nikkei.git`** で管理されている
- 実装・ドキュメント・UI 上の名称は、特段の理由がない限りこのリポジトリ名に揃える

## 目的

本リポジトリでは、**大規模言語モデル（LLM）** と **ニュース・エンティティ・市場データの類似度計算** を用いて、  
**日経225構成銘柄および日経平均系指標の翌営業日の騰落方向と予測変化率を推定するアプリ** を設計・実装する。

このファイルは、Codex / GPT 系エージェントが実装時に最優先で従う仕様書である。  
独断で大きくアーキテクチャを逸脱しないこと。

---

# 1. 開発方針

## 1.1 フェーズAを最優先にする

本プロジェクトは、最初から Python / FastAPI / PostgreSQL の本番構成を作るのではなく、  
**まず GitHub Pages で公開できるフロントエンド単体の MVP** を完成させる。

### フェーズA: 先行 MVP
- フロントエンドのみ
- 静的ホスティング
- GitHub Pages の `/docs` 公開
- ニュースデータ / エンティティ付き JSONL / 市場データ CSV をブラウザで読み込む
- Gemini API は **ユーザーが入力した API キー** を使って呼び出す
- API キーはブラウザの `localStorage` に保存してよい
- ただし **この方式はローカル検証・限定公開デモ専用**

### フェーズB: 本番移行
- Python
- FastAPI
- PostgreSQL + pgvector
- モデル呼び出しのサーバー側移管
- API キーをクライアントから除去
- 類似検索高速化
- バックテスト・継続運用

**Codex はまずフェーズAの完成を最優先にすること。**

## 1.2 MVP で避けること

- 先にバックエンド前提へ寄せること
- API キーの永続化方式を本番向けと誤認させること
- 実データを無視して抽象的なデータモデルだけ作ること
- ディレクトリ名だけで期間や内容を決め打ちすること

---

# 2. Gemini 利用方針

## 2.1 採用モデル

- 日次センチメント表現生成は **Gemini 3 Flash 系** を使う
- 現時点のローカル公式メモ `gemini-api-keys-official-docs.md` に合わせ、実装時の既定モデル名は **`gemini-3-flash-preview`** を優先する
- 旧設計の `Gemini 1.5 Flash` 前提は廃止する

## 2.2 structured output

- Gemini API の **structured output / JSON Schema** を前提に実装する
- 可能な限り自由文ではなく型付き JSON を返させる
- 少なくとも以下は structured output で受け取る
  - 日次センチメント表現
  - 予測理由
  - ニュース評価結果

## 2.3 API キーの扱い

- 初回起動時または未保存時に `Gemini API Key` 入力モーダルを表示する
- 保存先は `localStorage`
- ハードコードした API キーをソースコードへ埋め込んではならない
- README / AGENTS / UI 上で、**デモ用の暫定運用であり本番非推奨**と明記する

## 2.4 公式ドキュメントに基づく訂正事項

`gemini-api-keys-official-docs.md` のローカル参照内容に基づき、以下を正とする。

- モデル例は `gemini-3-flash-preview`
- REST 呼び出し時の API キーは `x-goog-api-key` ヘッダで渡す
- 環境変数例としては `GEMINI_API_KEY` が使われている
- JavaScript SDK の簡易例は環境にキーが供給されている前提に見えるが、**本 MVP のブラウザ実装ではユーザー入力キーを明示的に使う前提**で設計する

## 2.5 埋め込みの扱い

- 初期 MVP では、日次センチメント表現ベクトルを Gemini の structured output から得た数値特徴量で代用してよい
- 埋め込み API の導入は後続フェーズでよい
- ただし後で埋め込みモデルへ差し替え可能なデータ構造にしておくこと

## 2.6 エンティティ抽出

- 旧方式（ルールベース中心、旧 NER 中心）は廃止
- **Gemma 3 12B** をローカルで用いて、ニュース群からエンティティリストを抽出する方式を優先する
- Gemma 3 はローカル推論、別プロセス、外部前処理のいずれでもよい
- MVP では Gemma 3 の出力を **手動貼り付け / JSONL ファイル投入** できればよい

---

# 3. 公開方式

## 3.1 GitHub Pages 前提

- GitHub Pages で公開する
- 公開物は **`/docs`** に配置する
- エントリーポイントは **`docs/index.html`**
- 依存ビルドが必要でも、最終成果物は `/docs` に出力する

## 3.2 セキュリティ注意

- `localStorage` に API キーを保存する方式は **本番では絶対に採用しない**
- この方式は、ローカル検証または限定公開デモに限定する
- ユーザーがキー削除できる UI を必ず用意する

---

# 4. UI 設計方針

## 4.1 レスポンシブ必須

以下すべてで閲覧しやすいことを必須要件とする。

- スマートフォン
- タブレット
- PC

## 4.2 画面の基本構造

ページ全体は、**上から下に処理パイプラインが流れる UI** にする。  
ユーザーが今どの工程にいるか、一目で分かること。

### パイプライン
1. API キー入力
2. ニュース入力
3. 日次センチメント表現生成
4. エンティティ抽出結果入力 / 読み込み
5. 市場データ入力
6. 類似日計算
7. 予測出力表示

## 4.3 各フェーズの必須要素

各フェーズは以下を持つこと。

- 入力エリア
- 実行ボタン
- 保存済み状態表示
- スキップ / 再実行
- JSON 手動貼り付け
- ファイル読み込み

## 4.4 途中フェーズからの再開

以下のように途中から開始できること。

- ニュース入力済みなら、日次センチメント生成から開始
- センチメント生成済みなら、エンティティ入力から開始
- センチメントとエンティティがあるなら、市場データ投入から開始

## 4.5 レイアウト

- モバイル・タブレット・PC を通して、**主たる処理フローは基本 1 カラム** とする
- タブレット / PC では、ページ上部の hero / 要約領域に限って 2 カラムを許容する
- フェーズカード群はタブレット / PC でも 1 カラムを維持する
- ただし主軸の処理フローは必ず縦方向に維持する
- 各フェーズはカード UI で明確に分ける

## 4.6 フェーズカードの表示項目

- フェーズ番号
- フェーズ名
- 入力状態
- 実行状態
- 完了状態
- エラー状態
- 再実行ボタン

---

# 5. localStorage 方針

## 5.1 最低限の保存キー

以下を基準キーとする。

- `gemini_api_key`
- `app_state_pipeline`
- `daily_sentiment_representation`
- `entity_extraction_result`
- `market_similarity_input`

必要なら次も追加してよい。

- `news_input_raw`
- `news_input_metadata`
- `prediction_result`
- `last_loaded_files`

## 5.2 保存設計

- キー単位で意味を明確に分ける
- ファイル再読み込みなしで途中再開できるようにする
- JSON 文字列で保存し、復元時のバリデーションを行う

---

# 6. 実データ前提の入出力仕様

## 6.1 優先して対応する具体データ

MVP 実装では、少なくとも以下のファイルを読み込めること。

- `news_full_mcq3_type9_entities_novectors.jsonl`
- `market_data/2025-06-22_2025-06-28/market_data_all.csv`
- `market_data/2025-06-22_2025-06-28/market_data_requested_dates.csv`
- `market_data/2025-06-22_2025-06-28/fetch_report.json`

## 6.2 ニュース JSONL の実データ仕様

`news_full_mcq3_type9_entities_novectors.jsonl` は 1 行 1 JSON の JSONL 形式で、  
確認時点で **2159 件**のレコードを持つ。

### このファイルの特徴

- `provider_id` は全件 `kyodonews.jp`
- `status` は全件 `Usable`
- `canceled` は全件 `true`
- 各レコードに `questions` が 2 件ずつ存在する
- `named_entities` が既に付与されている
- ベクトルは含まれていない

### 実装上の注意

- **`canceled == true` だけを理由に全件除外してはならない**
- このファイルは既に前処理済みデータとして扱い、まずは UI で読めることを優先する
- 市場分析向けニュースだけに絞るフィルタは後段で追加してよいが、MVP では全件読込と日付単位集約を優先する

### 主なフィールド

- `date_id`
- `date_time`
- `headline`
- `sub_headline`
- `series_line`
- `content`
- `provider_id`
- `questions`
- `named_entities`
- `subject_codes`
- `first_created`
- `this_revision_created`
- `source_tar_entry`

### ニュースの集約単位

- 日次センチメント生成の基本単位は `date_id` 単位とする
- 日時の詳細が必要な場合は `date_time` または `this_revision_created` を使う
- `20250622` から `20250628` が中心だが、実データには `20250621` のレコードも少数含まれるため、**ファイル名や想定期間だけで決め打ちしない**

## 6.3 市場データ CSV の実データ仕様

`market_data/2025-06-22_2025-06-28/market_data_all.csv` は確認時点で **32 行**あり、  
以下 2 銘柄の時系列を含む。

- `^N225`
- `1306.T`

### カラム

- `ticker`
- `trade_date`
- `open`
- `high`
- `low`
- `close`
- `adj_close`
- `volume`
- `day_change`
- `day_change_pct`
- `prev_close_change`
- `prev_close_change_pct`

### 実装上の注意

- ディレクトリ名は `2025-06-22_2025-06-28` だが、実データの `trade_date` は **`2025-06-16` から `2025-07-07`** まで含む
- よって、**分析期間はフォルダ名ではなく CSV 内の `trade_date` から決める**
- `market_data_requested_dates.csv` は要求日だけを切り出したサブセットであり、`market_data_all.csv` が母集合
- `fetch_report.json` の `requested_dates` は `2025-06-23` から `2025-06-30`、`history_fetch_start` は `2025-06-16`、`history_fetch_end` は `2025-07-07`

## 6.4 fetch_report.json の扱い

`fetch_report.json` は UI 上で補助情報として使ってよい。

- 対象ティッカー一覧
- 要求日
- 履歴取得範囲
- 行数

この情報は表示用・バリデーション用に使ってよいが、**最終的な真実は CSV 本体の中身** とする。

---

# 7. 日次センチメント表現の新設計

## 7.1 廃止する旧方式

以下は廃止する。

- 1 日分ニュースをいったん 1 本の自由文サマリーに潰す方式
- その最終サマリーだけを埋め込み化する方式
- 「日次表現 = 単一サマリー埋め込み」という固定設計

## 7.2 新方式

**その日のニュース記事群をまとめて入力し、LLM が直接「日次センチメント表現」を構造化出力する** 方式にする。

### 入力に含めるもの

- 見出し
- 本文
- 配信日時
- ソース
- 任意メタ情報
- 既存の `named_entities` があれば補助入力として利用してよい

## 7.3 日次センチメント表現の考え方

「ポジティブ / ネガティブ」1 軸ではなく、  
**市場心理・材料・圧力の構造を多次元の数値特徴量として表現する。**

### 例

```json
{
  "trade_date": "2026-03-31",
  "market_regime": "risk_off",
  "overall_bias": -0.42,
  "confidence": 0.71,
  "dimensions": {
    "macro_tightening_pressure": 0.82,
    "domestic_policy_uncertainty": 0.41,
    "exporter_tailwind": 0.12,
    "semiconductor_momentum": -0.56,
    "consumer_strength": 0.08,
    "inflation_concern": 0.73,
    "yen_appreciation_pressure": 0.64,
    "energy_supply_risk": 0.17,
    "ai_theme_strength": 0.33,
    "financial_sector_support": -0.10
  },
  "dominant_themes": [
    "金融政策",
    "円高圧力",
    "半導体弱含み"
  ],
  "bullish_sectors": [
    "食品",
    "内需ディフェンシブ"
  ],
  "bearish_sectors": [
    "半導体",
    "輸出関連"
  ],
  "short_rationale": "当日のニュース群では金融引き締め懸念と円高圧力が優勢で、輸出関連と半導体に逆風がかかっている。"
}
```

## 7.4 実装上の要件

- 日付単位で JSON を生成・保存できること
- 手動貼り付けでも同じ JSON を受け取れること
- スキーマバリデーションを行うこと
- 数値特徴量は後段の類似度計算へ直接流せる形にすること

---

# 8. 類似日計算の MVP 方針

## 8.1 MVP で使う情報軸

MVP では以下 3 軸を扱える設計にする。

- 日次センチメント表現
- エンティティ集合
- 市場データ特徴量

## 8.2 MVP の簡略化

- 最初は厳密な pgvector / DTW 実装まで行かなくてよい
- ただし後で差し替えやすいよう、類似度関数は分離しておく
- 最低限、ニュース側特徴量と市場側特徴量を別物として扱うこと

## 8.3 予測出力

MVP の予測出力は、少なくとも以下を表示する。

- 対象日
- 類似日候補
- 類似スコア
- 予測方向
- 予測変化率
- 根拠の短文説明

最初は `^N225` と `1306.T` を対象にしてよい。  
日経225全構成銘柄への拡張は後続フェーズでよい。

---

# 9. 実装ルール

## 9.1 優先順位

1. まず `/docs` で動く MVP
2. 実データファイルの読込
3. structured output による日次表現生成
4. 類似日計算
5. 見やすい予測表示

## 9.2 禁止事項

- 本番向けセキュリティが満たされたかのような表現
- API キーの直書き
- 実データ形式を無視した独自フォーマットへの強制変換
- `canceled` フラグだけでニュース全除外
- ディレクトリ名だけで期間確定

## 9.3 推奨事項

- 入力ファイルのプレビューを UI に出す
- パース結果件数を表示する
- date ごとのニュース件数を見せる
- 生成した JSON をコピー / 保存 / 再読込できるようにする
- エラー時は「どのフェーズで失敗したか」を明示する

---

# 10. 迷ったときの判断基準

迷った場合は以下を優先すること。

1. フェーズAの MVP 完成
2. 実データをそのまま扱えること
3. Gemini 公式メモと矛盾しないこと
4. GitHub Pages で配布できること
5. 後でバックエンドへ移行しやすい分離設計
