# AGENTS.md

## 目的

本リポジトリでは、**大規模言語モデル（LLM）** と **マルチモーダル類似度計算** を用いて、  
**日経225構成銘柄の翌営業日の騰落方向および予測変化率を推定するアプリ** を設計・実装する。

このファイルは、Codex / GPT系エージェントが実装時に最優先で従う仕様書である。  
独断で大きくアーキテクチャを逸脱しないこと。

---

# 1. 重要な前提

## 1.1 開発フェーズの分離

本プロジェクトは、最初から Python / FastAPI / PostgreSQL の本番構成を作るのではなく、  
**まずは GitHub Pages で公開できるフロントエンド単体の試作版（MVP）** を先に作る。

その後、必要に応じてバックエンド構成へ段階的に移行する。

### フェーズA: 先行MVP
- フロントエンドのみ
- 静的ホスティング
- GitHub Pages `/docs` 公開
- Gemini API をブラウザから直接呼ぶ
- APIキーは初回入力モーダルから受け取り、`localStorage` に保存する
- 市場データ / ニュースデータは CSV / JSON / JSONL をブラウザで読み込む

### フェーズB: 本番移行
- Python
- FastAPI
- PostgreSQL + pgvector
- モデル呼び出しのサーバー側移管
- APIキーをクライアントから除去
- 類似検索高速化
- バックテスト・継続運用

**Codex はまずフェーズAの完成を最優先にすること。**

---

# 2. 使用モデル方針

## 2.1 要約・日次センチメント表現生成
- **Gemini 3 Flash** を使用する
- 旧設計の `Gemini 1.5 Flash` 前提は廃止する

## 2.2 structured output
- Gemini API の **structured output / JSON Schema** を前提に実装する
- 可能な限り、自由文ではなく、型付きJSONで返させる
- 日次センチメント表現、日次説明、ニュース評価結果は structured output で受け取る

## 2.3 埋め込みモデル
- 初期MVPでは、日次センチメント表現ベクトルを Gemini 3 Flash の structured output から生成した数値特徴量で代用してよい
- 埋め込みAPIの採用は後続フェーズで追加してよい
- ただし将来的には埋め込みモデルを差し替え可能な設計にすること

## 2.4 エンティティ抽出
- 旧方式（ルールベース中心、旧NER中心）は廃止
- **Gemma 3 12B** をローカルで用いて、ニュース群からエンティティリストを抽出する方式に変更する
- Gemma 3 はローカル推論、あるいは別プロセス / 別ツールとして扱ってよい
- MVPでは、Gemma 3 の出力を手動投入・ファイル投入できる設計でもよい

---

# 3. MVPの公開方式

## 3.1 配布先
- GitHub Pages で公開する
- 公開物は **`/docs` ディレクトリ** に配置する
- `docs/index.html` をエントリーポイントにする

## 3.2 GitHub Pages想定
- `main` ブランチの `/docs` を公開元にする構成を前提とする
- 依存ビルドが必要な場合も、最終成果物は `/docs` に出力する
- 可能な限り GitHub Pages 標準設定で動くようにする

## 3.3 セキュリティ注意
- APIキーを `localStorage` に保存する方式は、**あくまでローカル検証・限定公開デモ用**
- 本番では絶対に採用しない
- AGENTS / README / UI上で、この注意を明記すること

---

# 4. 画面設計方針

## 4.1 レスポンシブ必須
以下すべてで閲覧しやすいことを必須要件とする。

- スマートフォン
- タブレット
- PC

## 4.2 ページの基本構造
ページ全体は、**上から下に処理パイプラインが流れるUI** にする。

ユーザーが「今どの工程にいるか」が一目で分かるようにすること。

### 画面の流れ
1. APIキー入力
2. ニュース入力
3. 日次センチメント表現生成
4. エンティティ抽出結果入力 / 読み込み
5. 株価データ入力
6. 類似日計算
7. 予測出力表示

## 4.3 途中フェーズからの再開
ユーザーは、上から順に進めるだけでなく、以下のように**途中フェーズから開始可能**であること。

- フェーズ2から開始: すでにニュース入力済みで、日次センチメント生成から始める
- フェーズ3から開始: すでにセンチメント表現があり、エンティティ入力から始める
- フェーズ4から開始: すでにセンチメントとエンティティがあり、株価類似度計算から始める

そのため、各フェーズは以下を持つこと。

- 入力エリア
- 実行ボタン
- 保存済み状態表示
- スキップ / 再実行
- JSON の手動貼り付け
- ファイル読み込み

---

# 5. 初期UI仕様

## 5.1 APIキー入力モーダル
アプリ初回起動時、または APIキー未保存時にモーダルを表示する。

### 要件
- `Gemini API Key` の入力欄
- 保存ボタン
- 削除ボタン
- 「この方式はデモ用であり、本番では非推奨」の注意書き
- 保存先は `localStorage`

### localStorageのキー例
- `gemini_api_key`
- `app_state_pipeline`
- `daily_sentiment_representation`
- `entity_extraction_result`
- `market_similarity_input`

## 5.2 レイアウト
- モバイルでは1カラム
- タブレット / PCでは最大2カラム
- ただし処理フローの主軸は必ず縦方向に維持する
- 各フェーズはカードUIで区切る

## 5.3 視認性
各フェーズカードに以下を表示する。

- フェーズ番号
- フェーズ名
- 入力状態
- 実行状態
- 完了状態
- エラー状態
- 再実行ボタン

---

# 6. 日次センチメント表現の新設計

## 6.1 旧方式の廃止
以下の旧設計は廃止する。

- 「1日分ニュースをいったんサマリー化して、その最終サマリーだけを埋め込み化する」
- 「日次表現 = サマリー埋め込み」

## 6.2 新方式
**その日のニュース記事全文群をまとめて入力**し、  
**LLMが直接「日次センチメント表現」を構造化出力する**方式に変更する。

入力は以下とする。

- 見出し
- 本文
- 配信日時
- ソース
- 任意のメタ情報

出力は自由文ではなく、できる限り structured output にする。

## 6.3 日次センチメント表現の考え方
「ポジティブ / ネガティブ」1軸ではなく、  
**市場の心理・材料・圧力の構造を多次元の特徴量として表現する。**

### 例: 日次センチメント表現JSON
```json
{
  "trade_date": "2026-03-31",
  "market_regime": "risk_off",
  "overall_bias": -0.42,
  "confidence": 0.71,
  "dimensions": {
    "macro_tightening_pressure": 0.82,
    "domestic_policy_uncertainty": 0.41,
    "exporter_tailwind": 0.12,
    "semiconductor_momentum": -0.56,
    "consumer_strength": 0.08,
    "inflation_concern": 0.73,
    "yen_appreciation_pressure": 0.64,
    "energy_supply_risk": 0.17,
    "ai_theme_strength": 0.33,
    "financial_sector_support": -0.10
  },
  "dominant_themes": [
    "金融政策",
    "円高圧力",
    "半導体弱含み"
  ],
  "bullish_sectors": [
    "食品",
    "内需ディフェンシブ"
  ],
  "bearish_sectors": [
    "半導体",
    "輸出関連"
  ],
  "short_rationale": "..."
}