# AGENTS.md

## プロジェクト情報

- プロジェクト名 / リポジトリ名は **`daily-aura-predictor-nikkei`** を採用する
- Git リポジトリは **`https://github.com/Igaki12/daily-aura-predictor-nikkei.git`** で管理されている
- 実装・ドキュメント・UI 上の名称は、特段の理由がない限りこのリポジトリ名に揃える

## 目的

本リポジトリでは、**事前生成した日次表現データ** と **ニュース・エンティティ・市場データの類似度計算** を用いて、  
**日経225構成銘柄および日経平均系指標の翌営業日の騰落方向と予測変化率を推定するアプリ** を設計・実装する。

このファイルは、Codex / GPT 系エージェントが実装時に最優先で従う仕様書である。  
独断で大きくアーキテクチャを逸脱しないこと。

---

# 1. 開発方針

## 1.1 フェーズAを最優先にする

本プロジェクトは、最初から Python / FastAPI / PostgreSQL の本番構成を作るのではなく、  
**まず GitHub Pages で公開できるフロントエンド単体の MVP** を完成させる。

### フェーズA: 先行 MVP
- フロントエンドのみ
- **React + Vite** を採用する
- 静的ホスティング
- GitHub Pages の `/docs` 公開
- 開発用ソースは `src/` に置く
- Vite のビルド成果物を `docs/` に出力する
- `public/` に置いた公開用データは `docs/` に同梱される前提で扱う
- ニュースデータ / 事前生成済み日次センチメント表現 / 事前生成済み注目キーワード結果 / 市場データ CSV をブラウザで読み込む
- 概要資料 PDF を `public/` に置き、ブラウザ内モーダルから閲覧できるようにしてよい
- アプリ内で Gemini API キー入力や Gemini 呼び出しは行わない
- デモ時点では、ニュース対象日は **`2025-06-24` / `2025-06-25` / `2025-06-26`** の 3 日から選ぶ
- 後で別途用意する入力日ニュース記事データへ差し替えやすい構成を保つ

### フェーズB: 本番移行
- Python
- FastAPI
- PostgreSQL + pgvector
- モデル呼び出しのサーバー側移管
- API キーをクライアントから除去
- 類似検索高速化
- バックテスト・継続運用

**Codex はまずフェーズAの完成を最優先にすること。**

## 1.2 MVP で避けること

- 先にバックエンド前提へ寄せること
- ブラウザ内で API キーを要求する UI を残すこと
- 実データを無視して抽象的なデータモデルだけ作ること
- ディレクトリ名だけで期間や内容を決め打ちすること

---

# 2. 事前生成データ利用方針

## 2.1 基本方針

- 現在の MVP では、日次センチメント表現や注目キーワード集合を**アプリ内で生成しない**
- 必要な生成結果は **`public/precomputed/`** 配下の静的 JSON として事前に用意する
- UI 上では「計算結果を表示して確認する」形にし、その場でモデル推論しているように見せない

## 2.2 事前生成センチメント表現

- 事前生成の日次センチメント表現は、既存の structured output 互換 JSON Schema を維持してよい
- 少なくとも以下を持つこと
  - `trade_date`
  - `market_regime`
  - `overall_bias`
  - `confidence`
  - `dimensions`
  - `dominant_themes`
  - `bullish_sectors`
  - `bearish_sectors`
  - `short_rationale`
- UI 上では、単純なスコアではなく「市場の空気を表す構造化表現」として見せること

## 2.3 事前生成注目キーワード結果

- 注目キーワード結果も `public/precomputed/` 配下の JSON から読む
- 形式は少なくとも以下を満たすこと
  - `date_id`
  - `trade_date`
  - `named_entities`
  - `source`
- UI 文言は、必要以上に専門用語へ寄せず、**「注目キーワード」** を優先してよい

## 2.4 デモ版で必須の日付範囲

- 現行デモで Phase 2 の対象日として表示するのは以下の 3 日に固定する
  - `2025-06-24`
  - `2025-06-25`
  - `2025-06-26`
- 類似日比較用の事前生成データは、少なくとも以下の重なり日を揃えること
  - `2025-06-23`
  - `2025-06-24`
  - `2025-06-25`
  - `2025-06-26`
  - `2025-06-27`
- 今後、別で作成する入力日ニュース記事データを追加する場合も、同じスキーマと loader で差し替えられるようにする

## 2.5 埋め込みの扱い

- 初期 MVP では、日次センチメント表現ベクトルを事前生成 JSON に含まれる数値特徴量で代用してよい
- 埋め込み API の導入は後続フェーズでよい
- ただし後で埋め込みモデルへ差し替え可能なデータ構造にしておくこと
- ただし UI / ドキュメント上では、**単純な数値スコアではなく「センチメント表現」そのものを扱う** ことを強調する
- 数値だけで市場心理を圧縮する設計よりも、**意味のまとまりを保ったテキスト表現 + 構造化データ** を優先する

## 2.6 注目キーワード抽出の扱い

- 旧方式（ルールベース中心、旧 NER 中心）は廃止
- 注目キーワード抽出自体は、外部前処理・ローカル推論・別スクリプトのいずれでもよい
- ただし MVP のアプリ画面では、その場で抽出せず**事前生成済みの結果を確認する**形を優先する
- UI 上の文言は、必要以上に専門用語へ寄せず、**「注目キーワード」** を優先してよい

---

# 3. 公開方式

## 3.1 GitHub Pages 前提

- GitHub Pages で公開する
- 公開物は **`/docs`** に配置する
- エントリーポイントは **`docs/index.html`**
- 依存ビルドが必要でも、最終成果物は `/docs` に出力する
- Vite の `build.outDir` は `docs` を前提とする
- `public/` 配下の静的ファイルは GitHub Pages 公開対象として扱う
- 画像やサンプルデータを UI から直接読む場合、**最終的に `docs/` 配下へ出力されることを確認する**
- PDF のような補助資料も同様に、`public/` から `docs/` に出て GitHub Pages 上で参照できることを確認する

## 3.2 セキュリティ注意

- クライアントから API キー入力を要求しない
- 秘密情報を `public/` や `docs/` に含めてはならない
- `public/precomputed/` に置くのは公開可能な事前生成結果のみとする

---

# 4. UI 設計方針

## 4.1 レスポンシブ必須

以下すべてで閲覧しやすいことを必須要件とする。

- スマートフォン
- タブレット
- PC

## 4.2 画面の基本構造

ページ全体は、**上から下に処理パイプラインが流れる UI** にする。  
ユーザーが今どの工程にいるか、一目で分かること。

### パイプライン
1. データ準備
2. 対象日ニュース選択
3. 日次センチメント表現表示
4. 注目キーワード表示
5. 市場データ確認
6. 類似日計算
7. 予測出力表示

## 4.3 各フェーズの必須要素

各フェーズは以下を持つこと。

- 状態表示
- 主ボタン 1 つ
- 前工程完了時のみ操作可能な順番制御
- 必要に応じたプレビュー
- 再実行またはリセット導線

## 4.4 途中フェーズからの再開

以下のように途中から開始できること。

- データ準備済みなら、対象日ニュース選択から開始
- 対象日確定済みなら、日次センチメント表示から開始
- センチメント確認済みなら、注目キーワード表示から開始
- センチメントと注目キーワードが確認済みなら、市場データ確認から開始

## 4.5 レイアウト

- モバイル・タブレット・PC を通して、**主たる処理フローは基本 1 カラム** とする
- タブレット / PC では、ページ上部の hero / 要約領域に限って 2 カラムを許容する
- フェーズカード群はタブレット / PC でも 1 カラムを維持する
- ただし主軸の処理フローは必ず縦方向に維持する
- 各フェーズはカード UI で明確に分ける
- hero は独立カードというより、**ページ上部と連続した帯状のビジュアル** として扱ってよい
- hero の背景画像やビジュアル素材を使う場合、**hero 以外の文字色やカード配色を壊さないよう作用範囲を限定する**
- スマートフォン縦長画面では、hero とフェーズカードの横幅の見え方を揃えること
- スマートフォン縦長画面では、`body` の余白とカード幅の関係で横にはみ出さないようにすること
- モバイル向け幅調整では、フェーズカードの最大幅を viewport 基準で管理してよい
- hero や一般的な導線位置に、**「このアプリの概要」** のような補助ボタンを置いてよい
- 概要資料が PDF の場合、可能なら**ブラウザ内モーダル + iframe** で確認できるようにする

## 4.6 モーダル UI

- すべてのモーダルの「閉じる」に相当する操作は、**右上固定の閉じるアイコンボタン** を優先する
- 閉じるアイコンは PC / スマートフォンのどちらでも右上に見えること
- 概要資料 PDF モーダルの「別タブで開く」は、**外部リンクアイコン付きボタン** としてよい
- 外部リンクボタンは、モーダル本体とは異なる色で区別し、**モーダル右下** に配置してよい
- モーダル内の閲覧領域は、PC とスマートフォンの双方で無理なく収まること

## 4.7 フェーズカードの表示項目

- フェーズ番号
- フェーズ名
- 入力状態
- 実行状態
- 完了状態
- エラー状態
- 再実行ボタン

---

# 5. localStorage 方針

## 5.1 最低限の保存キー

以下を基準キーとする。

- `app_state_pipeline`
- `daily_sentiment_representation`
- `entity_extraction_result`
- `prediction_result`

必要なら次も追加してよい。

- `market_similarity_input`
- `news_input_raw`
- `news_input_metadata`

## 5.2 保存設計

- キー単位で意味を明確に分ける
- ページ再読み込みなしで途中再開できるようにする
- JSON 文字列で保存し、復元時のバリデーションを行う
- API キー保存は前提にしない

---

# 6. 実データ前提の入出力仕様

## 6.1 優先して対応する具体データ

MVP 実装では、少なくとも以下のファイルを読み込めること。

- `news_full_mcq3_type9_entities_novectors.jsonl`
- `precomputed/daily_sentiment_representations.json`
- `precomputed/entity_extraction_results.json`
- `market_data/2025-06-22_2025-06-28/market_data_all.csv`
- `market_data/2025-06-22_2025-06-28/market_data_requested_dates.csv`
- `market_data/2025-06-22_2025-06-28/fetch_report.json`
- `image-stock.jpg`
- `Glass_Box_Quant_Nikkei_Architecture(1).pdf`

これらは、必要に応じて `public/` に配置し、ビルド後に `docs/` から取得できる状態にすること。

## 6.2 ニュース JSONL の実データ仕様

`news_full_mcq3_type9_entities_novectors.jsonl` は 1 行 1 JSON の JSONL 形式で、  
確認時点で **2159 件**のレコードを持つ。

### このファイルの特徴

- `provider_id` は全件 `kyodonews.jp`
- `status` は全件 `Usable`
- `canceled` は全件 `true`
- 各レコードに `questions` が 2 件ずつ存在する
- `named_entities` が既に付与されている
- ベクトルは含まれていない

### 実装上の注意

- **`canceled == true` だけを理由に全件除外してはならない**
- このファイルは既に前処理済みデータとして扱い、まずは UI で読めることを優先する
- 市場分析向けニュースだけに絞るフィルタは後段で追加してよいが、MVP では全件読込と日付単位集約を優先する

### 主なフィールド

- `date_id`
- `date_time`
- `headline`
- `sub_headline`
- `series_line`
- `content`
- `provider_id`
- `questions`
- `named_entities`
- `subject_codes`
- `first_created`
- `this_revision_created`
- `source_tar_entry`

UI 上では `named_entities` をそのまま「エンティティ」と呼ぶ必要はなく、  
**「注目キーワード」** として見せてよい。

### ニュースの集約単位

- 日次センチメント生成の基本単位は `date_id` 単位とする
- 日時の詳細が必要な場合は `date_time` または `this_revision_created` を使う
- `20250622` から `20250628` が中心だが、実データには `20250621` のレコードも少数含まれるため、**ファイル名や想定期間だけで決め打ちしない**

## 6.3 市場データ CSV の実データ仕様

`market_data/2025-06-22_2025-06-28/market_data_all.csv` は確認時点で **32 行**あり、  
以下 2 銘柄の時系列を含む。

- `^N225`
- `1306.T`

### カラム

- `ticker`
- `trade_date`
- `open`
- `high`
- `low`
- `close`
- `adj_close`
- `volume`
- `day_change`
- `day_change_pct`
- `prev_close_change`
- `prev_close_change_pct`

### 実装上の注意

- ディレクトリ名は `2025-06-22_2025-06-28` だが、実データの `trade_date` は **`2025-06-16` から `2025-07-07`** まで含む
- よって、**分析期間はフォルダ名ではなく CSV 内の `trade_date` から決める**
- `market_data_requested_dates.csv` は要求日だけを切り出したサブセットであり、`market_data_all.csv` が母集合
- `fetch_report.json` の `requested_dates` は `2025-06-23` から `2025-06-30`、`history_fetch_start` は `2025-06-16`、`history_fetch_end` は `2025-07-07`

## 6.4 fetch_report.json の扱い

`fetch_report.json` は UI 上で補助情報として使ってよい。

- 対象ティッカー一覧
- 要求日
- 履歴取得範囲
- 行数

この情報は表示用・バリデーション用に使ってよいが、**最終的な真実は CSV 本体の中身** とする。

## 6.5 概要資料 PDF の扱い

- `Glass_Box_Quant_Nikkei_Architecture(1).pdf` はアプリ概要資料として扱ってよい
- `public/` に置き、ビルド後に `docs/Glass_Box_Quant_Nikkei_Architecture(1).pdf` として参照できること
- UI からは、hero など一般的な場所のボタン経由で開けるようにしてよい
- 可能ならモーダル内 `iframe` で閲覧できるようにする
- ブラウザ内表示が難しい環境向けに、別タブで開く導線も用意してよい
- 別タブ導線は、視認しやすい外部リンクボタンとして実装してよい

## 6.6 事前生成 JSON の扱い

- `precomputed/daily_sentiment_representations.json` は `date_id` ごとの日次センチメント表現を持つ
- `precomputed/entity_extraction_results.json` は `date_id` ごとの注目キーワード結果を持つ
- 現行デモで必要な最低対象日は `20250623` から `20250627`
- 選択対象日として UI に出すのは `20250624` / `20250625` / `20250626`
- 事前生成 JSON に不足日がある状態で、該当日の UI を有効化してはならない

---

# 7. 日次センチメント表現の扱い

## 7.1 廃止する旧方式

以下は廃止する。

- 1 日分ニュースをいったん 1 本の自由文サマリーに潰す方式
- その最終サマリーだけを埋め込み化する方式
- 「日次表現 = 単一サマリー埋め込み」という固定設計

## 7.2 現行 MVP の方式

**その日のニュース記事群に対応する事前生成 JSON を表示し、ユーザーが確認して次フェーズへ進む** 方式にする。

### 入力に含めるもの

- 見出し
- 本文
- 配信日時
- ソース
- 任意メタ情報
- 既存の `named_entities` があれば事前生成時の補助入力として利用してよい

## 7.3 日次センチメント表現の考え方

「ポジティブ / ネガティブ」1 軸ではなく、  
**市場心理・材料・圧力の構造を多次元の数値特徴量として表現する。**

加えて、**日次センチメントは単なる数値スコアではなく、意味のまとまりを保った「表現」** として扱う。  
実装・UI・説明文では、以下を優先すること。

- 単純な数値ランキングより、テーマ・市場心理・材料関係の説明を持つこと
- 後段の LLM 利用や埋め込み / ベクトル分析で意味を失いにくいこと
- 必要なら短い日本語の説明文を併置すること
- ユーザーに「数値化された感情」ではなく「市場の空気を表す構造化表現」だと伝わること

### 例

```json
{
  "trade_date": "2026-03-31",
  "market_regime": "risk_off",
  "overall_bias": -0.42,
  "confidence": 0.71,
  "dimensions": {
    "macro_tightening_pressure": 0.82,
    "domestic_policy_uncertainty": 0.41,
    "exporter_tailwind": 0.12,
    "semiconductor_momentum": -0.56,
    "consumer_strength": 0.08,
    "inflation_concern": 0.73,
    "yen_appreciation_pressure": 0.64,
    "energy_supply_risk": 0.17,
    "ai_theme_strength": 0.33,
    "financial_sector_support": -0.10
  },
  "dominant_themes": [
    "金融政策",
    "円高圧力",
    "半導体弱含み"
  ],
  "bullish_sectors": [
    "食品",
    "内需ディフェンシブ"
  ],
  "bearish_sectors": [
    "半導体",
    "輸出関連"
  ],
  "short_rationale": "当日のニュース群では金融引き締め懸念と円高圧力が優勢で、輸出関連と半導体に逆風がかかっている。"
}
```

## 7.4 実装上の要件

- 日付単位で JSON を読込・表示・保存できること
- スキーマバリデーションを行うこと
- 数値特徴量は後段の類似度計算へ直接流せる形にすること

---

# 8. 類似日計算の MVP 方針

## 8.1 MVP で使う情報軸

MVP では以下 3 軸を扱える設計にする。

- 日次センチメント表現
- 注目キーワード集合
- 市場データ特徴量

## 8.2 MVP の簡略化

- 最初は厳密な pgvector / DTW 実装まで行かなくてよい
- ただし後で差し替えやすいよう、類似度関数は分離しておく
- 最低限、ニュース側特徴量と市場側特徴量を別物として扱うこと
- センチメント軸は、現行 MVP では事前生成済みの日次センチメント表現同士を比較すること
- 市場比較は、**単日値をそのまま比較するよりも、数営業日の系列特徴量で比較する方式を優先する**
- 単日の `close` や `volume` の生値をそのまま類似度へ入れると、スケール差で市場スコアが壊れやすいので避ける
- 直近数営業日の変化率・レンジ率・出来高変化などを並べた系列ベクトルとして扱ってよい

## 8.3 予測出力

MVP の予測出力は、少なくとも以下を表示する。

- 対象日
- 類似日候補
- 類似スコア
- 予測方向
- 予測変化率
- 根拠の短文説明

候補比較のラベルは、必要以上に英語へ寄せず、日本語でよい。  
例:

- 総合
- センチメント
- 注目キーワード
- 市場

最初は `^N225` と `1306.T` を対象にしてよい。  
日経225全構成銘柄への拡張は後続フェーズでよい。

---

# 9. 実装ルール

## 9.1 優先順位

1. まず `/docs` で動く MVP
2. 実データファイルと事前生成ファイルの読込
3. 日次表現の表示確認フロー
4. 類似日計算
5. 見やすい予測表示

## 9.2 禁止事項

- 本番向けセキュリティが満たされたかのような表現
- API キーの直書き
- API キー入力モーダルや Gemini 実行 UI をデモ版に残すこと
- 実データ形式を無視した独自フォーマットへの強制変換
- `canceled` フラグだけでニュース全除外
- ディレクトリ名だけで期間確定

## 9.3 推奨事項

- 対象日のニュース件数と見出しプレビューを UI に出す
- パース結果件数を表示する
- date ごとのニュース件数を見せる
- 事前生成 JSON を整形表示して確認できるようにする
- エラー時は「どのフェーズで失敗したか」を明示する
- GitHub Pages 上で 404 が起きないよう、サンプル読込対象ファイルは `docs/` 配下へ実際に出ることを確認する
- 画像 URL やサンプルデータ URL は、相対パス解決のズレに注意し、**公開 URL で壊れない形**にする
- 長いファイル名や `code` 表示は、スマートフォン画面を押し広げないよう**折り返し可能**にする

---

# 10. 迷ったときの判断基準

迷った場合は以下を優先すること。

1. フェーズAの MVP 完成
2. 実データをそのまま扱えること
3. 事前生成データ前提の現行デモ仕様と矛盾しないこと
4. GitHub Pages で配布できること
5. 後でバックエンドへ移行しやすい分離設計
